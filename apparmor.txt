#include <tunables/global>

profile shelly_scanner flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Capabilities
  capability net_raw,
  capability net_admin,
  network inet tcp,
  network inet udp,
  network inet icmp,

  # S6-Overlay
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Access to options.json and other service settings
  /data/** rw,

  # Python
  /usr/bin/python3* ix,
  /usr/lib/python3*/** mr,

  # Application
  /app/** r,
  /app/app.py r,
  /app/static/** r,
  /app/templates/** r,

  # Home Assistant CLI
  /usr/bin/ha ix,

  # Access to SSL certificates
  /ssl/** r,

  # Network access for scanning
  /proc/sys/net/ipv4/ip_local_port_range r,
}
